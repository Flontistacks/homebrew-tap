# Flontistacks Homebrew Tap

Install [adl](https://github.com/Flontistacks/adl) with one command:

```bash
brew install Flontistacks/tap/adl
```

Latest development build:

```bash
brew install --HEAD Flontistacks/tap/adl
```
